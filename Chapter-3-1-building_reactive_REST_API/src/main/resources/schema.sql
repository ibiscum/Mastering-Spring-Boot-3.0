CREATE TABLE IF NOT EXISTS USERS (
                                     id BIGINT PRIMARY KEY AUTO_INCREMENT,
                                     name VARCHAR(100),
                                     email VARCHAR(100)
);

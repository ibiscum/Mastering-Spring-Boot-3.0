# Chapter 4.6 - Data Migration

    gradle wrapper --gradle-version latest

    docker run -t --rm -v ${PWD}:/app zavoloklom/dclint .

    docker compose up

    CREATE TABLE public.books ();

    ./gradlew bootRun

name: Chapter-4-2
services:
  db:
    image: postgres:18.3-alpine3.22
    environment:
      POSTGRES_PASSWORD: yourpassword
      POSTGRES_DB: bookstore
    ports:
      - '127.0.0.1:5432:5432'
    restart: always
